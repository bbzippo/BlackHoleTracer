<Window x:Class="BlackHoleUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BlackHoleUI"
        mc:Ignorable="d"
        
        SizeToContent="Manual" UseLayoutRounding="True" SnapsToDevicePixels="True"
        
        Title="MainWindow" Height="384" Width="436"
        
        Loaded="Window_Loaded" Closed="Window_Closed">

    <Grid>
        <Grid.LayoutTransform>
            <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
        </Grid.LayoutTransform>
        <TabControl>
            <TabItem Header="Settings">
                <Grid Margin="6">
                    <Grid.Resources>
                        <Style TargetType="RowDefinition">
                            <Setter Property="Height" Value="32"/>
                        </Style>

                        <Style TargetType="Label">
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="6"/>
                        </Style>

                        <Style TargetType="TextBox">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                        </Style>

                        <Style TargetType="Button">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="3" />
                            <Setter Property="Padding" Value="3" />
                        </Style>

                        <Style TargetType="ComboBox">
                            <Setter Property="HorizontalAlignment" Value="Left"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="IsEditable" Value="False"/>
                        </Style>

                        <Style TargetType="CheckBox">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                        </Style>
                    </Grid.Resources>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Label Grid.Row="0" Grid.Column="0" Target="{Binding ElementName=cbShowDisk}">Show Disk:</Label>
                    <CheckBox Grid.Row="0" Grid.Column="1" x:Name="cbShowDisk"  IsChecked="True" Click="cbShowDisk_Click"></CheckBox>
                    <Label Grid.Row="1" Grid.Column="0" Target="{Binding ElementName=cbShowBricks}">Show reference bricks:</Label>
                    <CheckBox Grid.Row="1" Grid.Column="1" x:Name="cbShowBricks"  IsChecked="True" Click="cbShowBricks_Click"></CheckBox>
                    
                    <Label Grid.Row="2" Grid.Column="0" Target="{Binding ElementName=cbbHorizon}">Horizon handling:</Label>
                    <ComboBox Grid.Row="2" Grid.Column="1" x:Name="cbbHorizon" SelectedIndex="0" Width="200" SelectionChanged="cbbHorizon_SelectionChanged">
                        <ComboBoxItem>Black</ComboBoxItem>
                        <ComboBoxItem>Reflective</ComboBoxItem>
                        <ComboBoxItem>Transparent</ComboBoxItem>
                    </ComboBox>
                    <Label Grid.Row="3" Grid.Column="0" Target="{Binding ElementName=tbBackground}">Background:</Label>
                    <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal">
                        <TextBox x:Name="tbBackground" Text="galaxy.png" Width="100" LostFocus="tbBackground_LostFocus" ></TextBox>
                        <Button x:Name="btnBackgroundFile" Content="File..." Click="btnBackgroundFile_Click"></Button>
                        <Label Target="{Binding ElementName=tbTiles}">Tiles:</Label>
                        <TextBox x:Name="tbTIles" Text="2" Width="24" LostFocus="tbTIles_LostFocus" ></TextBox>
                    </StackPanel>

                </Grid>
            </TabItem>
            <TabItem Header="Tweaks">
                <Grid Margin="6">
                    <Grid.Resources>
                        <Style TargetType="RowDefinition">
                            <Setter Property="Height" Value="32"/>
                        </Style>

                        <Style TargetType="Label">
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="6"/>
                        </Style>

                        <Style TargetType="TextBox">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                        </Style>

                        <Style TargetType="Button">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="3" />
                            <Setter Property="Padding" Value="3" />
                        </Style>

                        <Style TargetType="ComboBox">
                            <Setter Property="HorizontalAlignment" Value="Left"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="IsEditable" Value="False"/>
                        </Style>

                        <Style TargetType="CheckBox">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                        </Style>
                    </Grid.Resources>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Label Grid.Row="0" Grid.Column="0">Compute Width</Label>
                    <TextBox x:Name="txtCompW" Grid.Row="0" Grid.Column="1" Text="380" LostFocus="txtCompW_LostFocus"></TextBox>
                    <Label Grid.Row="1" Grid.Column="0">Compute Height</Label>
                    <TextBox x:Name="txtCompH" Grid.Row="1" Grid.Column="1" Text="380" LostFocus="txtCompW_LostFocus"></TextBox>
                    <Label Grid.Row="2" Grid.Column="0">Integration Steps</Label>
                    <TextBox x:Name="txtSteps" Grid.Row="2" Grid.Column="1" Text="8000" LostFocus="txtSteps_LostFocus"></TextBox>
                    <Label Grid.Row="3" Grid.Column="0">Integration Resolution</Label>
                    <TextBox x:Name="txtRes" Grid.Row="3" Grid.Column="1" Text="0.003" LostFocus="txtRes_LostFocus"></TextBox>
                    <Label Grid.Row="4" Grid.Column="0">Ray Escape distance</Label>
                    <TextBox x:Name="txtEscapeR" Grid.Row="4" Grid.Column="1" Text="40" LostFocus="txtEscapeR_LostFocus"></TextBox>

                    <Button Grid.Column="0" Grid.Row="6" x:Name="btnDefaultTweaks" Content="Safe defaults" Click="btnDefaultTweaks_Click"></Button>
                </Grid>
            </TabItem>

        </TabControl>


    </Grid>

</Window>
